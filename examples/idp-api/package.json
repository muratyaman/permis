{
  "name": "idp-api",
  "version": "1.0.0",
  "description": "sample IdP API",
  "main": "build/index.js",
  "scripts": {
    "clean": "rimraf lib",
    "build:tsc": "tsc --project ./tsconfig.build.json",
    "build": "npm run clean && npm run build:tsc",
    "start:dev": "ts-node ./src/index.ts",
    "start": "node .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "idp",
    "api",
    "identity",
    "provider"
  ],
  "author": "Haci Murat Yaman",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "redis": "^4.0.6"
  },
  "devDependencies": {
    "@types/body-parser": "^1.19.2",
    "@types/cors": "^2.8.12",
    "@types/express": "^4.17.13",
    "rimraf": "^3.0.2"
  }
}
